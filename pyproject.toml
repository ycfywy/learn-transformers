[project]
name = "transformers-lab"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "accelerate",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

